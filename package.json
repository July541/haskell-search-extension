{
  "scripts": {
    "test": "jest",
    "build": "npx ts-node -T build.ts --firefox",
    "build/dev": "npx ts-node -T build.ts --firefox --watch --dev",
    "run": "npx web-ext run -s dist-firefox -t chromium"
  },
  "devDependencies": {
    "@hanazuki/node-jsonnet": "^1.0.0",
    "@types/chrome": "^0.0.193",
    "@types/jest": "^28.1.5",
    "chokidar": "^3.5.3",
    "esbuild": "^0.14.49",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4",
    "web-ext": "^7.1.1"
  }
}
